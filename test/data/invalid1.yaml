apiVersion: iter8.tools/v2alpha3
kind: Metric
metadata:
  name: invalid-empty-params
spec:
  description: Invalid metric - empty params
  params:
  type: Gauge
  provider: provider
  jqExpression: ".data.result[0].value[1] | tonumber"
  url: url

